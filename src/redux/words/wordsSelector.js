export const allWords = (state) => state.words.words;
export const ownWords = (state) => state.words.ownWords;
export const statisticsWords = (state) => state.words.statistics;
export const resultWords = (state) => state.words.result;
export const pageWords = (state) => state.words.page;
export const perPageWords = (state) => state.words.perPage;
export const totalPagesWords = (state) => state.words.totalPages;
